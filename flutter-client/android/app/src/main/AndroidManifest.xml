<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Flutter only adds this to the debug manifest; a release build talks to
         the game server too. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- androidx.biometric comes in through google_sign_in_android's
         androidx.credentials and merges these two into every build. The game
         never asks for a fingerprint, and the Play listing would show the
         permission anyway, so they are taken out of the merged manifest
         (24 Sep 2026, owner's "fix all bugs"). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" tools:node="remove"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT" tools:node="remove"/>

    <!-- No Android backup of this app's data (24 Sep 2026, owner's "fix all
         bugs"). SharedPreferences hold the session JWT and the guest deviceId,
         which IS a guest's account (the server keys it on
         sha256('teenpatti:'+deviceId)); a Drive backup would carry both off
         the phone, and a device-to-device transfer would leave two phones
         signed in as one player, each replacing the other's session.
         allowBackup="false" stops cloud backup everywhere and device
         transfer before Android 12; dataExtractionRules stops device transfer
         on Android 12 and later, where allowBackup no longer does. The price:
         a guest who reinstalls starts a new account — Google sign-in is the
         way to keep one across phones. -->
    <application
        android:label="King Teen Patti"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Facebook login is switched off for now (owner, 23 Sep 2026): the
             flutter_facebook_auth dependency is commented out of pubspec.yaml,
             so these entries would name classes that are not in the build.
             Uncomment them together with it to bring Facebook back.

        Facebook Login.
             AutoInitEnabled is false on purpose: with it on, the SDK reads the
             app id at process start and complains when the strings are still
             empty, which is the state every build is in until somebody fills
             them. Off, it initialises on the first login attempt instead, and
             a build that carries no dart-define flag=FACEBOOK_APP_ID refuses
             that attempt in Dart before it ever reaches the SDK. (An XML
             comment may not contain a double hyphen, which is why the flag is
             written without its leading dashes.)
        <meta-data android:name="com.facebook.sdk.AutoInitEnabled" android:value="false" />
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id" />
        <meta-data android:name="com.facebook.sdk.ClientToken" android:value="@string/facebook_client_token" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:exported="false"
            android:label="King Teen Patti" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        -->
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
