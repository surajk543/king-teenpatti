<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom launcher manifest.

  This is a copy of Unity's own template (Editor/Data/PlaybackEngines/
  AndroidPlayer/Apk/LauncherManifest.xml) with two additions:

    * INTERNET, which the game obviously needs; and
    * usesCleartextTraffic, because Android has blocked plain HTTP by default
      since API 28, and a development server is reached over http:// — from an
      emulator that is http://10.0.2.2:3000.

  Only the launcher manifest is overridden. The Unity library manifest, which
  declares the player activity, is left alone so the choice between Activity and
  GameActivity stays with the Player Settings rather than being frozen here.

  For a production build pointing at an https:// server, drop
  usesCleartextTraffic — it is a development convenience, not something to ship.
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="preferExternal">
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:label="@string/app_name"
                 android:icon="@mipmap/app_icon"
                 android:usesCleartextTraffic="true"/>
</manifest>
